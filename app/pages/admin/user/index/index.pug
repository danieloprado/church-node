app-title Pessoas

md-button.md-fab.md-fab-bottom-right(ng-click="$ctrl.form(null, $event)", app-show-roles="admin")
  md-icon(md-svg-icon="plus",size=32)

md-card
  md-card-content(layout-padding)

    div(layout="column",layout-gt-xs="row")
      div(flex="60")
        md-input-container.md-block.m-t-none.m-b-none(md-no-float="")
          md-icon(md-svg-icon="magnify")
          input(ng-model='$ctrl.query.search', name='search', placeholder="Pesquisar")
      div(flex="30")
        P.text-right

    div.text-center.m-t-lg(ng-if="$ctrl.loading")
      md-progress-circular.inline-block(md-mode='indeterminate')

    md-subheader.p-none.text-right.md-no-sticky
      | Total de {{$ctrl.totals.total}} pessoas,
      |  sendo {{$ctrl.totals.members}} membros
      |  e {{$ctrl.totals.nonMembers}} cadastrados pelo app

md-card.m-t-md
  md-tabs(md-dynamic-height, md-border-bottom)
    md-tab(label="Membros")
      app-user-list-component(users="$ctrl.users", query="$ctrl.query", is-member="true")

    md-tab(label="Cadastrados pelo App")
      app-user-list-component(users="$ctrl.users", query="$ctrl.query", is-member="false")