form(md-form-validator,ng-submit='$ctrl.recoveryAccess()')
  md-card
    md-card-content
      p.p-b-sm.m-none Iremos lhe enviar um email para recuperar seu acesso
      md-input-container.md-block
        label Email
        input(ng-model='$ctrl.model.email', name='email', type='email', required)
        md-messages

  div(layout='row')
      md-button.md-raised(ng-click="$ctrl.currentTab = 0") Voltar
      span(flex='')
      md-button.md-raised.md-accent(type='submit') Enviar